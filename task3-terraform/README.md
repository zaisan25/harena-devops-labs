# Infrastructure as Code with Terraform (Docker Provider)

This directory automates the deployment of the `devops-control-plane` container using Terraform.

## Deployment Lifecycle Commands

### 1. Initialization
Downloads the required Docker provider plugin.
```bash
terraform init